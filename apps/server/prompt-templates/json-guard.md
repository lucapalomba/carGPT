# RESPOND EXCLUSIVELY WITH VALID JSON.

- Your entire response MUST be a single, well-formed JSON object or array.
- DO NOT include any explanations, comments, or additional text outside the JSON.
- DO NOT wrap the JSON in Markdown code fences (e.g., ```json).
- Ensure all string values and special characters (e.g., double quotes, backslashes, control characters) within the JSON are properly escaped according to JSON standards.
- JSON keys and string values MUST use double quotes only. DO NOT use single quotes.